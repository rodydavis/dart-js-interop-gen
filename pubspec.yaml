name: dart_js_interop_gen
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  source_gen: ^0.9.6
  build: ^1.3.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
